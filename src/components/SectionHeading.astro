---
type Props = {
  id?: string;
  title: string;
  text: string;
  align?: "left" | "center";
};

const { id, title, text, align = "left" } = Astro.props;
---

<div class:list={["section-heading", `section-heading--${align}`]} id={id}>
  <h2>{title}</h2>
  <p>{text}</p>
</div>
